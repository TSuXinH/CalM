defaults:
  - vq_train
  - data: Tseng_trial_small
  - model: model
  - loss: loss
  - trainer: trainer
  - optim: adamw
  - _self_

remark: "0225_hydra"

runtime:
  device: "cuda"
  tf32: true
  matmul_precision: "high"
  compile: true
  seed: 0

logging:
  plot_dir: null
  plot_every: 1

paths:
  output_root: ""
  ckpt_dir: ""

hydra:
  job:
    chdir: false
  run:
    dir: ""
